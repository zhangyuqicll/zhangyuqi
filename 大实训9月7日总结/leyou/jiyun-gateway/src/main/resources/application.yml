server:
  port: 10010 #服务器端口
spring:
  application:
    name: jiyun-gateway #指定服务名
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka
    registry-fetch-interval-seconds: 5

  prefix: /api #路由器路径前缀